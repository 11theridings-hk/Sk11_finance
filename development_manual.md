# Finance J18 開發手冊

## 1. 文件定位

本文件是 `finance-J18new` 專案的主開發手冊，用途有三個：

1. 作為目前程式的真實現況說明，避免後續開發建立在過時文件上。
2. 作為後續新增功能的規格母版，讓需求可以持續往下疊加。
3. 作為交給 TRAE IDE 或其他 Agent 執行開發工作的依據文件。

本版文件更新日期：`2026-08-23`

---

## 2. 專案目標

這是一套以手機端為主、桌面端可用的財務記錄系統，主要服務場景為：

- 多角色密碼登入，不使用帳號名稱。
- 管理收入、支出、應收款、應付款。
- 透過分類、資金池、附件、歸結單管理財務資料。
- 透過管理員審核流程控制部分記錄入帳。
- 產出報表、PDF 匯出與打包下載。

未來目標分兩段：

- 第一段：先把業務功能做完整、穩定、可測試。
- 第二段：再封裝成可在 Windows 電腦上安裝與部署的應用方案。

---

## 3. 目前真實技術現況

以下內容以目前程式碼為準，不再沿用舊版手冊中的過時描述。

### 3.1 技術棧

- 前端／全端框架：`Next.js 16.3.0` + `React 19.2.8`
- 路由模式：`App Router`
- ORM：`Prisma 6.19.3`
- 資料庫：`PostgreSQL`
- 樣式：`Tailwind CSS 4`
- 匯出：`jsPDF`、`jspdf-autotable`、`JSZip`
- 認證：`JWT` + `HttpOnly Cookie`
- 佈署模式：`Next standalone` + `Dockerfile`

### 3.2 重要更正

- 舊文件提到 `SQLite`，但實際 `prisma/schema.prisma` 已改為 `PostgreSQL`。
- 專案目前是 Web 全端架構，不是桌面 App 架構。
- 專案目前沒有 `Electron`、`Tauri`、`WiX`、`NSIS`、`Squirrel` 等 Windows 原生安裝包配置。

### 3.3 目錄重點

- `src/app/`：頁面、Client Components、Server Actions
- `src/lib/prisma.ts`：Prisma Client 單例
- `prisma/schema.prisma`：資料模型
- `public/fonts/`：PDF 匯出所需中文字型
- `Dockerfile`：容器建置
- `deploy.sh`：Ubuntu/PM2 佈署腳本
- `install_env.sh`：伺服器環境初始化

---

## 4. 系統角色與權限

### 4.1 一般使用者

- 可登入系統。
- 可新增收入、支出、應收、應付。
- 可查看自己的首頁、報表、應收應付、歸結單。
- 可提出收入／支出記錄的修改申請。
- 不可進入管理後台與審核頁。

### 4.2 管理員

- 擁有一般使用者全部能力。
- 可登入管理模式。
- 可查看所有使用者與全部報表資料。
- 可管理分類、附件、資金池、使用者。
- 可審核待審記錄與修改申請。
- 可對歸結單執行結單。

---

## 5. 已實作功能地圖

### 5.1 路由

- `/login`：登入頁
- `/`：首頁／記帳工作台
- `/ar-ap`：應收應付管理
- `/consolidated`：歸結單管理
- `/report`：報表查詢與匯出
- `/review`：管理員審核頁
- `/admin`：管理後台
- `/api/init`：初始化超級管理員

### 5.2 首頁功能

- 顯示 HKD / RMB 的現金流、應收、應付總覽。
- 可切換一般收支模式、應收應付模式、歸結單模式。
- 可建立收入或支出。
- 可建立應收或應付記錄。
- 可選擇建立新歸結單或掛入既有歸結單。
- 支援附件圖片上傳，前端壓縮至約 `200KB` 以下。
- 提交前有二次確認與 `5 秒` 倒數防誤觸。
- 顯示最近記錄與可選歸結單。

### 5.3 應收應付頁

- 查詢 AR / AP 記錄。
- 修改 AR / AP 金額。
- 為 AR / AP 新增 remark log 跟進備註。

### 5.4 歸結單頁

- 查看開啟中的歸結單。
- 查看已結單資料。
- 查看單內明細。
- 管理員可結單。

### 5.5 報表頁

- 依日期、分類、幣別、角色查詢資料。
- 僅查詢 `APPROVED` 資料。
- 可匯出明細列表 PDF。
- 可匯出逐筆會計明細 PDF 並打包成 ZIP。
- 可對收入／支出記錄提出修改申請。

### 5.6 審核頁

- 管理員可查看待審、已審、駁回記錄。
- 可審核新建記錄。
- 可審核修改申請副本。
- 通過時會同步處理資金池餘額。

### 5.7 管理後台

- 分類管理
  - 主分類 / 子分類
  - 建立 / 刪除
  - 刪除後資料移轉至「未分類」
- 附件管理
  - 查看附件圖
  - 顯示上傳者、分類、大小
- 資金池管理
  - 建立 / 刪除資金池
  - 支援審核型資金池
- 使用者管理
  - 建立 / 修改 / 刪除使用者
  - 設定角色名稱
  - 啟用 / 停用專屬資金池

---

## 6. 核心資料模型

### 6.1 User

- `password`：唯一登入憑證，實作上以 hash 儲存
- `roleName`：角色名稱
- `isAdmin`：是否管理員
- `poolEnabled`：是否啟用專屬資金池

### 6.2 Category

- `name`：分類名稱
- `type`：`INCOME` 或 `EXPENSE`
- `parentId`：支援兩層分類

### 6.3 CapitalPool

- `name`：資金池名稱
- `balanceHkd` / `balanceRmb`：雙幣別餘額
- `userId`：綁定角色，可為空
- `isReviewRequired`：是否送審

### 6.4 ConsolidatedOrder

- `orderNo`：唯一歸結單號
- `date`
- `note`
- `status`：`OPEN` / `CLOSED`
- `closedAt`

### 6.5 Record

- `type`：`INCOME` / `EXPENSE` / `AR` / `AP`
- `status`：`PENDING` / `APPROVED` / `REJECTED`
- `date`
- `executionDate`：AR/AP 使用
- `note`
- `currency`：`HKD` / `RMB`
- `amount`
- `attachmentUrl`
- `originalRecordId`：修改送審時指向原始記錄
- `isReviewing`：原始記錄是否正在審核中
- `userId` / `categoryId` / `subCategoryId` / `poolId` / `orderId`

### 6.6 RemarkLog

- AR/AP 跟進備註紀錄

### 6.7 Attachment

- 附件路徑
- 檔案大小
- 上傳者
- 關聯分類

---

## 7. 關鍵業務規則

### 7.1 登入規則

- 以密碼直接登入，不需帳號。
- 使用 `JWT` 寫入 `session_token`。
- Cookie 為 `HttpOnly`，有效期 `24h`。
- 系統支援舊資料明文密碼自動升級成 hash。

### 7.2 收支入帳規則

- `INCOME` 金額必須為正。
- `EXPENSE` 金額必須為負。
- 只有收支類型會直接影響資金池餘額。
- 若選到 `isReviewRequired = true` 的資金池，記錄狀態為 `PENDING`。
- 只有 `APPROVED` 的收支記錄才會真正更新資金池。

### 7.3 AR / AP 規則

- `AR` 與 `AP` 不綁定資金池。
- `AR` / `AP` 預設直接 `APPROVED`。
- AR/AP 的後續追蹤依賴 `RemarkLog`。

### 7.4 歸結單規則

- 建立記錄時若帶入 `orderNo`，會自動建立或掛入歸結單。
- 若歸結單已經 `CLOSED`，不可再追加記錄。

### 7.5 修改送審規則

- 報表頁修改不是直接改原記錄。
- 系統會建立一筆 `PENDING` 副本。
- 原記錄會標記 `isReviewing = true`。
- 管理員核准後才覆寫原記錄並重算資金池。
- 管理員駁回後，原記錄恢復為可再次申請狀態。

### 7.6 分類刪除規則

- 系統會確保存在「未分類」。
- 被刪除分類的主分類引用轉移至「未分類」。
- 被刪除主分類的子分類升為根分類。

### 7.7 資金池規則

- 使用者啟用專屬資金池時，系統會自動建立同名資金池。
- 修改角色名稱時，若該角色已有專屬資金池，資金池名稱需同步更新。
- 有關聯記錄的資金池不可刪除。

---

## 8. 開發環境與執行方式

### 8.1 必要環境變數

建立 `.env`：

```env
DATABASE_URL="postgresql://user:password@localhost:5432/finance"
JWT_SECRET="your-super-secret-jwt-key"
PWD_SALT="your-password-salt"
INIT_SECRET="your-init-secret"
```

### 8.2 本機開發流程

```bash
npm ci
npx prisma migrate deploy
npx prisma generate
npm run dev
```

### 8.3 建置流程

```bash
npm run build
```

目前實際 build script 為：

```bash
prisma generate && next build
```

### 8.4 初始化管理員

首次部署後，透過：

```text
/api/init?secret=INIT_SECRET
```

建立預設超級管理員，再以管理員入口登入。

---

## 9. UI / UX 準則

- 以手機端優先，但桌面端可正常操作。
- 高資訊密度，避免大面積空白與過度裝飾。
- 主色基調維持 iOS 風格淺灰底、白卡片、藍色主操作。
- 收入與支出需維持明確視覺區分。
- 重要操作必須有二次確認。
- 提交、刪除、審核、修改送審需有明確成功/失敗回饋。
- 表單填寫流程優先順手而不是純視覺炫技。

---

## 10. 已知文件與程式落差

以下是目前需要修正或後續補強的地方。

### 10.1 文件落差

- 舊版手冊與 README 仍有 `SQLite` 描述，需全面改為目前的 `PostgreSQL` 現況，並另外標示桌面版將改用本機資料庫。
- 舊版手冊沒有完整納入 AR/AP、歸結單、修改送審、審核流程。
- 舊版手冊沒有納入「原生離線桌面版」目標。

### 10.2 產品與工程待補強

- 缺少明確自動化測試。
- 缺少 `.env.example`。
- 缺少正式的發版流程文件。
- 缺少資料備份／還原流程。
- 附件目前為單一 `attachmentUrl` 模式，無法支援多檔案與後續追加。
- 分類目前僅支援兩層，無法滿足三層關聯分類。
- 幣別目前為雙幣，與新需求「單一 HKD」不一致。
- 全站目前未導入真正的 i18n 架構。

---

## 11. 產品目標調整

根據最新需求，產品定位正式調整如下：

- 主要交付目標：`Windows 原生桌面版`
- 使用方式：`單機離線使用`
- 多人同時使用：`暫不需要`
- 線上版：`不是第一階段目標`

### 11.1 對架構的直接影響

這代表後續開發不應只延續目前的「Web 系統 + 伺服器資料庫」思路，而要分成兩層看待：

1. 現有程式仍是功能原型基礎，可延續 UI、流程與大部分業務邏輯。
2. 最終交付形態應轉為桌面容器，並採本機資料庫與本機檔案存放。

### 11.2 桌面版建議技術方向

建議目標架構：

- 桌面殼：`Electron`
- UI：沿用 `Next.js / React`
- 本機資料庫：`SQLite` 或等效本機嵌入式資料庫
- ORM：可保留 `Prisma`，但需重新調整 datasource
- 附件儲存：本機檔案目錄，不再使用單一欄位存 base64
- 安裝包：Windows `.exe` / `.msi`

### 11.3 為什麼改成桌面版後，資料庫策略也要改

目前系統用 `PostgreSQL`，這適合伺服器或多人共用場景；但若目標是：

- 單機使用
- 離線使用
- 原生桌面安裝

則更適合改為本機 `SQLite`：

- 安裝簡單，不需要另外安裝資料庫伺服器
- 備份就是備份資料檔與附件目錄
- 沒有網路暴露面，安全性與維運都較單純
- 對單人桌面工具的效能已足夠

### 11.4 對「是否做線上版」的評估

如果未來要支援線上使用，複雜度會明顯增加，原因包括：

- 需要正式的伺服器部署與監控
- 需要多人權限與併發資料一致性處理
- 需要 HTTPS、網路安全、備份、災難復原
- 需要處理外網登入、暴力破解、防火牆與日誌
- 附件與資料庫的備份、權限、還原都更複雜

結論：

- `離線單機桌面版` 複雜度明顯較低，較適合你目前目標。
- `線上多人版` 不是不能做，但應視為第二產品階段，而不是順手加一點設定就完成。

### 11.5 資料安全觀點

#### 離線桌面版的優點

- 沒有外網暴露面，不會直接遭受線上攻擊。
- 不依賴雲端，資料完全留在本機。
- 適合敏感財務資料的單機使用。

#### 離線桌面版的風險

- 若電腦中毒、遺失、硬碟損毀，資料仍可能消失。
- 若沒有備份機制，安全性只等於「這台電腦是否安全」。

#### 必做補強

- 本機資料加密或至少敏感設定保護
- 自動備份 / 匯出備份
- 附件與資料庫一致性備份
- 啟動密碼 / 管理密碼保護

---

## 12. 已確認的新功能規格批次

以下 7 項需求已列為下一輪正式規格。

### 12.1 功能 1：歸結單變成可選功能

#### 目標

讓管理員可在後台決定是否啟用歸結單模組；若關閉，使用者不應看見相關入口。

#### 使用者

- 管理員：可設定開關
- 一般使用者：依設定決定是否可見

#### 使用流程

1. 管理員在後台進入「系統設定」或「功能開關」區塊。
2. 切換「啟用歸結單」開關。
3. 關閉後，首頁的歸結單模式、導覽、`/consolidated` 頁面入口全部隱藏。
4. 若使用者直接輸入 URL，系統需阻擋並導回首頁或顯示未啟用提示。

#### 後端 / 資料調整

- 新增全域設定表，例如 `SystemSetting`
- 增加 `consolidatedOrderEnabled` 布林設定

#### 驗收標準

- [ ] 後台可開啟 / 關閉歸結單
- [ ] 關閉後前端不顯示歸結單相關 UI
- [ ] 關閉後不可直接存取歸結單頁面
- [ ] 既有歸結單歷史資料保留，不因關閉而刪除

### 12.2 功能 2：分類改為 3 層

#### 目標

目前分類只有 2 層，需提升為 3 層關聯分類。

#### 設計原則

- 層級最多 3 層
- 不可無限巢狀
- UI 必須清楚顯示父 / 子 / 孫分類

#### 影響

- `Category` 資料結構雖可延續 `parentId`，但前後端邏輯都要改
- `Record` 目前只有 `categoryId` 與 `subCategoryId`，需擴充第三層欄位

#### 後端 / 資料調整

建議新增：

- `thirdCategoryId` 或 `thirdLevelCategoryId`

#### 驗收標準

- [ ] 後台可新增第 3 層分類
- [ ] 表單可依序選擇第 1 / 2 / 3 層分類
- [ ] 報表與列表可正確顯示完整分類路徑
- [ ] 刪除分類時仍能正確轉移或清理關聯

### 12.3 功能 3：新增合約功能

#### 目標

建立一個接近 AR/AP 結構的新模組「合約」，但不處理結算與入帳，只處理合約管理與到期提醒。

#### 需求重點

- 內容結構可參考 AR/AP
- 不用結算金額
- 可保留金額欄位作為參考資訊
- 新增 `合約開始日` 與 `到期日`
- 合約列表以「接近到期日」排序
- 卡片底色依接近程度改變

#### 建議狀態欄位

- `ACTIVE`
- `EXPIRING_SOON`
- `EXPIRED`
- `ARCHIVED`

#### 建議模組內容

- 合約列表頁
- 合約建立 / 編輯 / 詳情
- 到期排序與顏色提示
- 後續可擴充提醒功能

#### 後端 / 資料調整

建議新增 `Contract` 模型，欄位至少包括：

- `id`
- `referenceNo`
- `contractNo`
- `title`
- `counterparty`
- `startDate`
- `expiryDate`
- `reminderDays`
- `amount`（選填）
- `note`
- `attachment` 關聯
- `status`
- `createdBy`

#### 驗收標準

- [ ] 可新增合約
- [ ] 可編輯合約
- [ ] 合約頁可依到期日排序
- [ ] 接近到期的卡片顏色會改變
- [ ] 已過期合約可清楚識別

### 12.4 功能 4：雙幣改為單一 HKD

#### 目標

整個系統統一使用 `HKD`，移除 `RMB` 與多幣別概念。

#### 影響範圍

- `Record.currency`
- `CapitalPool.balanceHkd / balanceRmb`
- 首頁統計
- 報表篩選
- PDF 匯出
- 所有 UI 顯示

#### 建議處理方式

- 新版本只保留 `HKD`
- 舊資料中的 `RMB` 需定義遷移策略

#### 遷移策略待定

已確認策略：

- 舊有 `RMB` 資料不帶入新桌面版
- 新桌面版從 `HKD-only` 結構重新開始
- 若要保留舊資料，應另外提供匯出封存方案，不混入新桌面版正式資料庫

#### 驗收標準

- [ ] 全站不再出現幣別選擇
- [ ] 所有金額顯示統一為 HKD
- [ ] 報表與匯出不再有幣別篩選
- [ ] 舊資料遷移策略已明確落地

### 12.5 功能 5：附件上傳改為多檔案、多格式

#### 目標

附件系統需改為可上傳多個檔案，且支援照片與 PDF 等格式。

#### 設計方向

- 一筆交易可綁多個附件
- 檔案格式至少支援：圖片、PDF
- 桌面版應以本機檔案儲存為主

#### 後端 / 資料調整

目前 `Record` 只有單一 `attachmentUrl`，需改為：

- `Record` 與 `Attachment` 改成一對多
- `Attachment` 需新增 `recordId`
- 補充 `mimeType`、`fileName`、`filePath`、`fileExt`

#### 驗收標準

- [ ] 建立交易時可上傳多個附件
- [ ] 附件可含圖片與 PDF
- [ ] 可預覽或至少開啟附件
- [ ] 附件清單可顯示檔名與格式
- [ ] 附件增多後，PDF / ZIP 匯出排版仍正確

### 12.6 功能 6：已儲存記錄可後續追加附件與編輯

#### 目標

任何已儲存賬單都可透過參考編號查找，再追加上傳多個附件與管理附件。

#### 需求重點

- 每筆交易要有唯一 `參考編號`
- 可用參考編號搜尋交易
- 搜尋到後可追加附件
- 可編輯附件內容或刪除附件

#### 後端 / 資料調整

建議在 `Record` 新增：

- `referenceNo @unique`

並新增：

- 依 `referenceNo` 查詢記錄的流程
- 記錄附件管理 action

#### 驗收標準

- [ ] 每筆交易建立時自動產生參考編號
- [ ] 使用者可用參考編號搜尋交易
- [ ] 搜尋後可追加多個附件
- [ ] 已存在附件可刪除或管理

### 12.7 功能 7：支援繁中 / 簡中 / 英文

#### 目標

整個系統可切換：

- 繁體中文
- 簡體中文
- English

#### 設計方向

- 導入正式 i18n 字典架構
- 所有 UI 文案不得再硬編碼在元件內
- 使用者可在設定中切換語言

#### 後端 / 資料調整

建議新增使用者設定欄位：

- `preferredLanguage`

或全域設定預設語言。

#### 驗收標準

- [ ] 可切換三種語言
- [ ] 主要頁面文案完整切換
- [ ] 表單提示、按鈕、錯誤訊息都可切換
- [ ] 不可出現部分頁面未翻譯

### 12.8 功能依賴與建議順序

這 7 項需求之間有依賴關係，建議順序如下：

1. 功能 4：雙幣改單幣
2. 功能 2：分類升級為 3 層
3. 功能 5：多附件模型
4. 功能 6：參考編號與後續追加附件
5. 功能 1：歸結單開關
6. 功能 3：合約模組
7. 功能 7：三語系

原因：

- 幣別、分類、附件模型屬底層資料結構，應先改
- 合約模組可重用 AR/AP 與附件能力
- 多語系要改全站文案，放後面較適合

---

## 13. 原生桌面版發行策略

### 13.1 最終方向

Windows 目標改為：

- `原生桌面安裝版`
- `單機離線使用`
- `非多人共用`

### 13.2 建議方案

建議使用：

- `Electron` 作為桌面容器
- 現有 `Next.js` UI 作為 renderer 端基礎
- 本機 `SQLite` 作為正式桌面版資料庫
- 本機附件資料夾作為檔案存放位置

### 13.3 為什麼不建議直接把現在的 Web 專案硬包成原生桌面版

因為目前專案仍假設：

- 有伺服器
- 有 PostgreSQL
- 附件邏輯偏向 Web 流程

若硬包，會導致：

- 安裝包內還要塞資料庫服務
- 啟動與更新機制複雜
- 檔案路徑與權限問題變多

因此桌面版應做「架構收斂」：

- 從 Web 伺服器式模型，收斂成單機本地資料模型

### 13.4 桌面版最小可行交付內容

第一版桌面交付至少應包含：

- 安裝程式
- 本機資料庫初始化
- 本機附件資料夾初始化
- 匯出 / 備份功能
- 還原功能
- 語言切換

### 13.5 桌面版需新增的安全能力

- 應用程式啟動密碼或管理者密碼
- 手動備份匯出
- 還原備份
- 重要設定與資料目錄說明
- 附件與資料庫同批備份

---

## 14. 後續功能開發方式

本手冊之後會用「功能批次」方式持續擴寫。

每新增一個功能，必須補齊以下 8 項資訊：

1. 功能名稱
2. 背景目的
3. 使用角色
4. 操作入口頁面
5. 欄位與規則
6. 權限與審核條件
7. 資料表 / 欄位調整
8. 驗收條件

### 14.1 功能需求模板

後續每次要新增功能，請用以下格式補進來：

```md
## 功能名稱

### 目標
- 為什麼要做這個功能

### 使用者
- 哪些角色可以使用

### 使用流程
1. 使用者從哪個頁面進入
2. 做了哪些操作
3. 系統應該如何回應

### 欄位規格
- 欄位名稱：
- 型別：
- 必填 / 選填：
- 預設值：
- 驗證規則：

### 業務規則
- 規則 1
- 規則 2

### 畫面調整
- 需要改哪些頁面或元件

### 後端調整
- 需要改哪些 server action / schema / 查詢

### 驗收標準
- [ ] 情境 1
- [ ] 情境 2
- [ ] 情境 3
```

---

## 15. 建議開發里程碑

### Phase 1：架構決策與資料模型重整

- 確定桌面版技術路線：`Electron + SQLite`
- 明確定義現有 PostgreSQL 到桌面版本機資料庫的轉換策略
- 單幣 HKD 重構
- 三層分類重構
- 多附件資料模型重構
- 交易參考編號機制

### Phase 2：核心功能擴充

- 歸結單功能開關
- 合約模組
- 已儲存交易追加附件
- 附件搜尋與管理流程

### Phase 3：全站語系化與桌面能力

- 三語系字典
- 使用者語言設定
- 本機附件目錄管理
- 備份 / 還原

### Phase 4：桌面安裝版交付

- Electron 整合
- Windows 安裝包
- 本機資料初始化
- 升級 / 備份說明
- 發版文件

---

## 16. 交給 TRAE IDE 的執行準則

當本手冊交給 TRAE IDE 執行時，應遵守以下順序：

1. 先閱讀本文件。
2. 以目前程式碼為基礎，但以「桌面版目標」為未來方向。
3. 不可直接假設保留現有 PostgreSQL 作為最終桌面版資料庫。
4. 每次開發須先確認是否屬於「資料模型重整」或「功能新增」。
5. 若功能牽涉附件、幣別、分類、桌面存檔路徑，必須先檢查是否影響其他模組。
6. 優先做底層資料結構改造，再做 UI 外觀延伸。
7. 每次改動後至少驗證：
   - 登入
   - 新增記錄
   - 附件上傳
   - 報表查詢
   - 審核流程
   - 受影響的匯出功能

### 16.1 不建議隨意重構的區域

- 認證模式的使用者體驗
- 資金池審核規則
- 修改送審資料模型
- 報表 PDF 匯出流程
- 歸結單歷史資料語意

### 16.2 必須優先重整的區域

- 幣別模型
- 分類層級模型
- 附件模型
- Record 參考編號
- i18n 文案結構
- 桌面版資料存放策略

---

## 17. 驗收清單

每一批功能完成後，至少檢查以下項目：

- [ ] 一般使用者可正常登入
- [ ] 管理員可正常登入
- [ ] 首頁可新增收入 / 支出
- [ ] 全站只剩 HKD
- [ ] 三層分類可正常建立與選取
- [ ] 一筆交易可綁多個附件
- [ ] 已儲存交易可依參考編號追加附件
- [ ] 合約模組可正常建立與排序
- [ ] 歸結單可被後台開關控制
- [ ] AR / AP 可建立與追蹤
- [ ] 審核型資金池會產生待審記錄
- [ ] 管理員可完成審核
- [ ] 報表查詢結果正確
- [ ] PDF / ZIP 匯出正常
- [ ] 三語系切換完整
- [ ] 新增功能對桌面版封裝沒有造成未評估衝擊

---

## 18. 目前仍待你最終確認的事項

以下幾點雖然已有方向，但仍建議在正式開發前最後確認：

### 18.1 單幣遷移策略

- 已確認：舊有 `RMB` 資料不帶入新桌面版
- 若要保存歷史，應輸出成封存資料或舊版備份，不與新桌面版主資料庫混用

### 18.2 合約欄位細節

目前已確認：

- 有開始日
- 有到期日
- 有合約編號
- 有合約對象
- 有提醒天數
- 有狀態欄位
- 可留金額

### 18.3 三語系切換策略

需確認：

- 預設語言是否為繁中
- 是否要記住每位使用者的語言偏好
- 匯出 PDF 要跟隨哪個語言

### 18.4 桌面版資料安全策略

- 已確認第一階段先做：
- 應用啟動密碼
- 手動備份匯出

- 暫未確認是否加入：
- 備份檔加密
- 自動每日備份

---

## 19. 本文件維護原則

- 任何新功能開始前，先更新手冊。
- 任何資料表調整後，必須同步更新手冊。
- 任何封裝策略變更後，必須同步更新手冊。
- 若 README 與本文件衝突，以本文件與實際程式碼為準，但應盡快同步修正 README。
