# Finance J18 開發手冊

## 1. 文件定位

本文件是 `finance-J18new` 專案的主開發手冊，用途有三個：

1. 作為目前程式的真實現況說明，避免後續開發建立在過時文件上。
2. 作為後續新增功能的規格母版，讓需求可以持續往下疊加。
3. 作為交給 TRAE IDE 或其他 Agent 執行開發工作的依據文件。

本版文件更新日期：`2026-08-23`

---

## 2. 專案目標

這是一套以手機端為主、桌面端可用的財務記錄系統，主要服務場景為：

- 多角色密碼登入，不使用帳號名稱。
- 管理收入、支出、應收款、應付款。
- 透過分類、資金池、附件、歸結單管理財務資料。
- 透過管理員審核流程控制部分記錄入帳。
- 產出報表、PDF 匯出與打包下載。

未來目標分兩段：

- 第一段：先把業務功能做完整、穩定、可測試。
- 第二段：再封裝成可在 Windows 電腦上安裝與部署的應用方案。

---

## 3. 目前真實技術現況

以下內容以目前程式碼為準，不再沿用舊版手冊中的過時描述。

### 3.1 技術棧

- 前端／全端框架：`Next.js 16.3.0` + `React 19.2.8`
- 路由模式：`App Router`
- ORM：`Prisma 6.19.3`
- 資料庫：`PostgreSQL`
- 樣式：`Tailwind CSS 4`
- 匯出：`jsPDF`、`jspdf-autotable`、`JSZip`
- 認證：`JWT` + `HttpOnly Cookie`
- 佈署模式：`Next standalone` + `Dockerfile`

### 3.2 重要更正

- 舊文件提到 `SQLite`，但實際 `prisma/schema.prisma` 已改為 `PostgreSQL`。
- 專案目前是 Web 全端架構，不是桌面 App 架構。
- 專案目前沒有 `Electron`、`Tauri`、`WiX`、`NSIS`、`Squirrel` 等 Windows 原生安裝包配置。

### 3.3 目錄重點

- `src/app/`：頁面、Client Components、Server Actions
- `src/lib/prisma.ts`：Prisma Client 單例
- `prisma/schema.prisma`：資料模型
- `public/fonts/`：PDF 匯出所需中文字型
- `Dockerfile`：容器建置
- `deploy.sh`：Ubuntu/PM2 佈署腳本
- `install_env.sh`：伺服器環境初始化

---

## 4. 系統角色與權限

### 4.1 一般使用者

- 可登入系統。
- 可新增收入、支出、應收、應付。
- 可查看自己的首頁、報表、應收應付、歸結單。
- 可提出收入／支出記錄的修改申請。
- 不可進入管理後台與審核頁。

### 4.2 管理員

- 擁有一般使用者全部能力。
- 可登入管理模式。
- 可查看所有使用者與全部報表資料。
- 可管理分類、附件、資金池、使用者。
- 可審核待審記錄與修改申請。
- 可對歸結單執行結單。

---

## 5. 已實作功能地圖

### 5.1 路由

- `/login`：登入頁
- `/`：首頁／記帳工作台
- `/ar-ap`：應收應付管理
- `/consolidated`：歸結單管理
- `/report`：報表查詢與匯出
- `/review`：管理員審核頁
- `/admin`：管理後台
- `/api/init`：初始化超級管理員

### 5.2 首頁功能

- 顯示 HKD / RMB 的現金流、應收、應付總覽。
- 可切換一般收支模式、應收應付模式、歸結單模式。
- 可建立收入或支出。
- 可建立應收或應付記錄。
- 可選擇建立新歸結單或掛入既有歸結單。
- 支援附件圖片上傳，前端壓縮至約 `200KB` 以下。
- 提交前有二次確認與 `5 秒` 倒數防誤觸。
- 顯示最近記錄與可選歸結單。

### 5.3 應收應付頁

- 查詢 AR / AP 記錄。
- 修改 AR / AP 金額。
- 為 AR / AP 新增 remark log 跟進備註。

### 5.4 歸結單頁

- 查看開啟中的歸結單。
- 查看已結單資料。
- 查看單內明細。
- 管理員可結單。

### 5.5 報表頁

- 依日期、分類、幣別、角色查詢資料。
- 僅查詢 `APPROVED` 資料。
- 可匯出明細列表 PDF。
- 可匯出逐筆會計明細 PDF 並打包成 ZIP。
- 可對收入／支出記錄提出修改申請。

### 5.6 審核頁

- 管理員可查看待審、已審、駁回記錄。
- 可審核新建記錄。
- 可審核修改申請副本。
- 通過時會同步處理資金池餘額。

### 5.7 管理後台

- 分類管理
  - 主分類 / 子分類
  - 建立 / 刪除
  - 刪除後資料移轉至「未分類」
- 附件管理
  - 查看附件圖
  - 顯示上傳者、分類、大小
- 資金池管理
  - 建立 / 刪除資金池
  - 支援審核型資金池
- 使用者管理
  - 建立 / 修改 / 刪除使用者
  - 設定角色名稱
  - 啟用 / 停用專屬資金池

---

## 6. 核心資料模型

### 6.1 User

- `password`：唯一登入憑證，實作上以 hash 儲存
- `roleName`：角色名稱
- `isAdmin`：是否管理員
- `poolEnabled`：是否啟用專屬資金池

### 6.2 Category

- `name`：分類名稱
- `type`：`INCOME` 或 `EXPENSE`
- `parentId`：支援兩層分類

### 6.3 CapitalPool

- `name`：資金池名稱
- `balanceHkd` / `balanceRmb`：雙幣別餘額
- `userId`：綁定角色，可為空
- `isReviewRequired`：是否送審

### 6.4 ConsolidatedOrder

- `orderNo`：唯一歸結單號
- `date`
- `note`
- `status`：`OPEN` / `CLOSED`
- `closedAt`

### 6.5 Record

- `type`：`INCOME` / `EXPENSE` / `AR` / `AP`
- `status`：`PENDING` / `APPROVED` / `REJECTED`
- `date`
- `executionDate`：AR/AP 使用
- `note`
- `currency`：`HKD` / `RMB`
- `amount`
- `attachmentUrl`
- `originalRecordId`：修改送審時指向原始記錄
- `isReviewing`：原始記錄是否正在審核中
- `userId` / `categoryId` / `subCategoryId` / `poolId` / `orderId`

### 6.6 RemarkLog

- AR/AP 跟進備註紀錄

### 6.7 Attachment

- 附件路徑
- 檔案大小
- 上傳者
- 關聯分類

---

## 7. 關鍵業務規則

### 7.1 登入規則

- 以密碼直接登入，不需帳號。
- 使用 `JWT` 寫入 `session_token`。
- Cookie 為 `HttpOnly`，有效期 `24h`。
- 系統支援舊資料明文密碼自動升級成 hash。

### 7.2 收支入帳規則

- `INCOME` 金額必須為正。
- `EXPENSE` 金額必須為負。
- 只有收支類型會直接影響資金池餘額。
- 若選到 `isReviewRequired = true` 的資金池，記錄狀態為 `PENDING`。
- 只有 `APPROVED` 的收支記錄才會真正更新資金池。

### 7.3 AR / AP 規則

- `AR` 與 `AP` 不綁定資金池。
- `AR` / `AP` 預設直接 `APPROVED`。
- AR/AP 的後續追蹤依賴 `RemarkLog`。

### 7.4 歸結單規則

- 建立記錄時若帶入 `orderNo`，會自動建立或掛入歸結單。
- 若歸結單已經 `CLOSED`，不可再追加記錄。

### 7.5 修改送審規則

- 報表頁修改不是直接改原記錄。
- 系統會建立一筆 `PENDING` 副本。
- 原記錄會標記 `isReviewing = true`。
- 管理員核准後才覆寫原記錄並重算資金池。
- 管理員駁回後，原記錄恢復為可再次申請狀態。

### 7.6 分類刪除規則

- 系統會確保存在「未分類」。
- 被刪除分類的主分類引用轉移至「未分類」。
- 被刪除主分類的子分類升為根分類。

### 7.7 資金池規則

- 使用者啟用專屬資金池時，系統會自動建立同名資金池。
- 修改角色名稱時，若該角色已有專屬資金池，資金池名稱需同步更新。
- 有關聯記錄的資金池不可刪除。

---

## 8. 開發環境與執行方式

### 8.1 必要環境變數

建立 `.env`：

```env
DATABASE_URL="postgresql://user:password@localhost:5432/finance"
JWT_SECRET="your-super-secret-jwt-key"
PWD_SALT="your-password-salt"
INIT_SECRET="your-init-secret"
```

### 8.2 本機開發流程

```bash
npm ci
npx prisma migrate deploy
npx prisma generate
npm run dev
```

### 8.3 建置流程

```bash
npm run build
```

目前實際 build script 為：

```bash
prisma generate && next build
```

### 8.4 初始化管理員

首次部署後，透過：

```text
/api/init?secret=INIT_SECRET
```

建立預設超級管理員，再以管理員入口登入。

---

## 9. UI / UX 準則

- 以手機端優先，但桌面端可正常操作。
- 高資訊密度，避免大面積空白與過度裝飾。
- 主色基調維持 iOS 風格淺灰底、白卡片、藍色主操作。
- 收入與支出需維持明確視覺區分。
- 重要操作必須有二次確認。
- 提交、刪除、審核、修改送審需有明確成功/失敗回饋。
- 表單填寫流程優先順手而不是純視覺炫技。

---

## 10. 已知文件與程式落差

以下是目前需要修正或後續補強的地方：

### 10.1 文件落差

- 舊版手冊與 README 仍有 `SQLite` 描述，需全面改為 `PostgreSQL`。
- 舊版手冊沒有納入 AR/AP、歸結單、修改送審、審核流程。
- 舊版手冊沒有 Windows 打包策略。

### 10.2 產品與工程待補強

- 缺少明確自動化測試。
- 缺少 `.env.example`。
- 缺少 `docker-compose.yml`，無法直接作為 Windows Docker 發行包。
- 缺少正式的發版流程文件。
- 缺少資料備份／還原流程。

---

## 11. Windows 發行策略

使用者最終目標是「可在 Windows 電腦上安裝使用」，但目前程式架構是 Web 全端 + PostgreSQL，因此必須先選擇封裝策略。

### 11.1 方案 A：Docker Desktop 發行包

這是目前最推薦方案。

#### 優點

- 保留現有 Next.js + Prisma + PostgreSQL 架構，不需大改。
- 與現有 `Dockerfile` 相容度高。
- 易於部署、搬遷、備份與版本升級。
- 可同時支援 Linux 伺服器與 Windows 開發/安裝環境。

#### 缺點

- 使用者電腦需要先安裝 Docker Desktop。
- 不屬於純原生 Windows 單檔安裝體驗。

#### 建議交付內容

- `Dockerfile`
- `docker-compose.yml`
- `.env.example`
- `README-Windows-Docker.md`
- 一鍵啟動 / 停止腳本

### 11.2 方案 B：Windows Node 服務版

以 Windows 安裝 Node.js + PostgreSQL，再啟動 Next standalone 服務。

#### 優點

- 比 Docker 更接近原生主機部署。

#### 缺點

- 環境依賴多，維護與安裝失敗率高。
- Windows 服務註冊、環境變數、資料庫初始化都較繁瑣。
- 不適合作為第一優先的對外安裝包。

### 11.3 方案 C：原生 Windows 桌面 App

使用 `Electron` 或 `Tauri` 封裝成 `.exe` / `.msi`。

#### 結論

這不是目前建議立即進行的方案，原因如下：

- 現有專案不是桌面架構。
- 需重新定義本機資料庫策略。
- 需重新處理桌面端安裝、更新、資料路徑、離線能力。
- 開發成本與風險都遠高於方案 A。

### 11.4 當前建議

先完成業務功能與測試，再做：

1. `Windows Docker 版` 作為第一個可安裝交付版本。
2. 若未來有明確需求，再評估 `Electron/Tauri` 原生桌面版。

---

## 12. 後續功能開發方式

本手冊之後會用「功能批次」方式持續擴寫。

每新增一個功能，必須補齊以下 8 項資訊：

1. 功能名稱
2. 背景目的
3. 使用角色
4. 操作入口頁面
5. 欄位與規則
6. 權限與審核條件
7. 資料表 / 欄位調整
8. 驗收條件

### 12.1 功能需求模板

後續每次要新增功能，請用以下格式補進來：

```md
## 功能名稱

### 目標
- 為什麼要做這個功能

### 使用者
- 哪些角色可以使用

### 使用流程
1. 使用者從哪個頁面進入
2. 做了哪些操作
3. 系統應該如何回應

### 欄位規格
- 欄位名稱：
- 型別：
- 必填 / 選填：
- 預設值：
- 驗證規則：

### 業務規則
- 規則 1
- 規則 2

### 畫面調整
- 需要改哪些頁面或元件

### 後端調整
- 需要改哪些 server action / schema / 查詢

### 驗收標準
- [ ] 情境 1
- [ ] 情境 2
- [ ] 情境 3
```

---

## 13. 建議開發里程碑

### Phase 1：文件與配置補齊

- 修正 README 技術棧描述
- 補 `.env.example`
- 補資料備份說明
- 補 Windows Docker 發行文件

### Phase 2：功能擴充

- 依你後續提出的功能清單逐項補規格
- 每一批功能都先更新本手冊，再實作程式

### Phase 3：品質補強

- 加入關鍵流程測試
- 盤點權限邏輯
- 盤點報表與匯出正確性
- 盤點 Prisma migration 策略

### Phase 4：Windows 交付

- 補 `docker-compose.yml`
- 補 Windows 啟動腳本
- 補安裝步驟與故障排除
- 產出第一版 Windows 安裝文件

---

## 14. 交給 TRAE IDE 的執行準則

當本手冊交給 TRAE IDE 執行時，應遵守以下順序：

1. 先閱讀本文件。
2. 以目前程式碼為準，不可回退到舊版 SQLite 假設。
3. 先確認本次要做的功能批次。
4. 先更新手冊中的「功能需求模板區」，再開始改程式。
5. 實作時優先維持現有 App Router + Server Actions 架構。
6. 若需求會影響封裝策略，必須先回報，不可直接硬做。
7. 每次改動後至少驗證：
   - 登入
   - 新增記錄
   - 報表查詢
   - 審核流程
   - 受影響的匯出功能

### 14.1 不建議隨意重構的區域

- 認證模式
- 資金池審核規則
- 修改送審資料模型
- 報表 PDF 匯出流程
- 歸結單關聯規則

### 14.2 可以優先整理的區域

- README
- 環境變數樣板
- 測試與驗證腳本
- Docker 發行文件
- 後續功能規格區塊

---

## 15. 驗收清單

每一批功能完成後，至少檢查以下項目：

- [ ] 一般使用者可正常登入
- [ ] 管理員可正常登入
- [ ] 首頁可新增收入 / 支出
- [ ] AR / AP 可建立與追蹤
- [ ] 審核型資金池會產生待審記錄
- [ ] 管理員可完成審核
- [ ] 報表查詢結果正確
- [ ] PDF 匯出正常
- [ ] ZIP 打包正常
- [ ] 歸結單新增與結單正常
- [ ] 管理後台 CRUD 正常
- [ ] 新增功能對 Windows 發行策略沒有造成未評估衝擊

---

## 16. 下一步待確認事項

目前這份手冊已完成「現況盤點 + 技術路線 + 封裝策略 + 開發模板」，但若要進入真正的功能開發，還需要你補以下資訊：

1. 你接下來最想新增的 `3 ~ 5 個功能` 是什麼。
2. Windows 交付你要的是：
   - `Docker 安裝版`
   - `Windows 服務版`
   - `原生桌面安裝版`
3. 這套系統是否需要：
   - 多人同時使用
   - 區域網部署
   - 離線使用
   - 自動更新
4. 報表是否還要增加：
   - Excel 匯出
   - 圖表儀表板
   - 月結 / 年結
   - 權限隔離查詢

---

## 17. 本文件維護原則

- 任何新功能開始前，先更新手冊。
- 任何資料表調整後，必須同步更新手冊。
- 任何封裝策略變更後，必須同步更新手冊。
- 若 README 與本文件衝突，以本文件與實際程式碼為準，但應盡快同步修正 README。
