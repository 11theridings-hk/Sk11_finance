# TRAE IDE 執行任務單（Finance J18）

## 使用方式

1. 每次只執行一個 Batch。
2. 每個 Batch 完成後先回報變更清單、風險與測試結果，再執行下一個。
3. 任何資料模型變更都要附 migration 策略與回滾方案。
4. 以 `development_manual.md` 最新版本為唯一需求基準。

---

## 全域執行規則（每個 Batch 都要遵守）

- 嚴禁回退到舊版假設（例如固定雙幣、兩層分類、單附件）。
- 保留既有登入、審核、報表主流程，不可破壞現有功能。
- 每次修改後，至少驗證：
  - 登入（一般/管理員）
  - 新增交易
  - 報表查詢
  - 審核流程
  - 匯出流程（受影響時）
- 所有新增欄位與資料遷移都要補到文件。

---

## Batch 1：單幣 HKD + 三層分類

### 任務目標

- 全站由雙幣改為單幣 `HKD`。
- 分類由 2 層提升至 3 層（父/子/孫）。

### 必做項目

- 調整 Prisma schema：
  - 移除或停用 `RMB` 相關欄位與流程（依手冊策略）
  - `Record` 增加第三層分類關聯欄位（例如 `thirdCategoryId`）
- 調整 Server Actions：
  - `record/category/report` 相關邏輯支援三層分類
  - 報表查詢可正確處理三層條件與顯示
- 調整前端頁面：
  - 首頁表單、管理後台分類、報表頁都支援三層分類選取
  - 移除幣別選擇 UI，統一顯示 HKD
- 調整匯出：
  - PDF 內容改為單幣 HKD，移除雙幣區塊

### 驗收標準

- [ ] 全站不再有幣別切換
- [ ] 三層分類可建立/選取/顯示
- [ ] 報表與匯出可正確顯示三層分類與 HKD
- [ ] 舊 RMB 資料不進入新桌面版資料主路徑（依手冊）

### 回報格式

- 變更檔案清單
- Prisma migration 說明
- 手動測試結果
- 風險與待辦

---

## Batch 2：多附件 + 參考編號 + 追加附件

### 任務目標

- 一筆交易可綁多個附件（圖片/PDF）。
- 每筆交易有唯一參考編號，可搜尋後追加/管理附件。

### 必做項目

- 調整 Prisma schema：
  - `Record` 新增 `referenceNo @unique`
  - `Attachment` 加入 `recordId` 關聯與檔案資訊欄位（`fileName/mimeType/filePath/fileExt`）
  - `Record` 與 `Attachment` 建立一對多
- 調整新增交易流程：
  - 建立交易時可一次上傳多個附件
- 新增附件管理流程：
  - 依 `referenceNo` 搜尋交易
  - 可對既有交易追加附件與刪除附件
- 調整匯出流程：
  - 多附件情境下，PDF / ZIP 排版與生成邏輯仍可讀可用

### 驗收標準

- [ ] 建立交易可上傳多附件
- [ ] 可搜尋參考編號找到交易
- [ ] 可對既有交易追加附件
- [ ] 匯出（PDF/ZIP）在多附件情境下正常

### 回報格式

- 變更檔案清單
- 附件儲存策略說明（目前 Web / 未來桌面）
- 匯出排版調整說明
- 測試結果與已知限制

---

## Batch 3：歸結單開關 + 合約模組

### 任務目標

- 歸結單變成管理後台可開關功能。
- 新增合約模組（有開始日、到期日、提醒天數、狀態、合約編號、對象）。

### 必做項目

- 新增系統設定（例如 `SystemSetting`）：
  - `consolidatedOrderEnabled`
- 歸結單開關整合：
  - 關閉時隱藏首頁歸結單 UI、隱藏導覽入口、阻擋 `/consolidated` 直入
- 新增 `Contract` 模型與功能：
  - 欄位：`contractNo/counterparty/startDate/expiryDate/reminderDays/status/amount/note/...`
  - 合約列表依接近到期排序
  - 卡片底色依到期接近程度變化

### 驗收標準

- [ ] 後台可切換歸結單開關，且前後端同步生效
- [ ] 合約可新增、編輯、列表顯示
- [ ] 合約可依到期排序與變色提示
- [ ] 關閉歸結單後不可繞過 UI 直接使用頁面

### 回報格式

- 變更檔案清單
- 新增資料表與欄位說明
- 合約排序與顏色規則說明
- 測試結果

---

## Batch 4：三語系 + 桌面版骨架

### 任務目標

- 全站支援繁中 / 簡中 / 英文切換。
- 建立 Windows 原生離線桌面版第一版骨架（Electron 方向）。

### 必做項目

- 導入 i18n 結構：
  - 建立語系資源檔（zh-Hant / zh-Hans / en）
  - 移除硬編碼文案
  - 新增語言切換入口
- 使用者語言偏好：
  - 新增 `preferredLanguage`（或等效設定）
- 建立桌面版骨架：
  - Electron 啟動流程
  - 本機資料與附件路徑規劃
  - 啟動密碼機制（第一版）
  - 手動備份匯出（第一版）

### 驗收標準

- [ ] 三語切換可覆蓋主要頁面、表單、提示
- [ ] 語言偏好可保存
- [ ] Electron 可啟動應用
- [ ] 有最小可用的啟動密碼與手動備份流程

### 回報格式

- 變更檔案清單
- i18n 架構說明
- 桌面版骨架與路線圖
- 未完成項目與下一步

---

## 每個 Batch 完成後固定回覆模板（請 TRAE IDE 使用）

```md
## Batch X 完成回報

### 完成項目
- ...

### 變更檔案
- ...

### 資料模型 / Migration
- ...

### 測試結果
- ...

### 風險與限制
- ...

### 下一個 Batch 建議
- ...
```
